% preamble.tex — packages, macros, and theorem environments
% Target: SciPost Physics (primary) / PRB fallback

\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{bm}          % bold math
%\usepackage{physics}     % braket notation, \dd, \tr, etc. (not installed)
%\usepackage{siunitx}     % physical units
\usepackage{caption}
\usepackage{subcaption}
\usepackage{multirow}
\usepackage{microtype}
\usepackage{fancyhdr}
\setlength{\headheight}{16pt}

% ── Demo watermark header/footer ──────────────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.8pt}
\renewcommand{\footrulewidth}{0.4pt}
\definecolor{demoRed}{RGB}{180,0,0}
\fancyhead[C]{%
  \textcolor{demoRed}{\textbf{\small%
    [DEMO]\enspace This paper was completed by Claude Sonnet 4.6 using Amplify Skillsets
    $\cdot$ No human editing $\cdot$ For demonstration purposes only $\cdot$ Please treat with caution%
  }}%
}
\fancyfoot[C]{%
  \textcolor{demoRed}{\small%
    Generated by Claude Sonnet 4.6 via Amplify Skillsets
    $\cdot$ No human editing $\cdot$ For demonstration only
    $\cdot$ \thepage%
  }%
}
% Keep fancy style on the first page (which uses \maketitle)
\fancypagestyle{plain}{%
  \fancyhf{}%
  \renewcommand{\headrulewidth}{0.8pt}%
  \renewcommand{\footrulewidth}{0.4pt}%
  \fancyhead[C]{%
    \textcolor{demoRed}{\textbf{\small%
      [DEMO]\enspace This paper was completed by Claude Sonnet 4.6 using Amplify Skillsets
      $\cdot$ No human editing $\cdot$ For demonstration purposes only $\cdot$ Please treat with caution%
    }}%
  }%
  \fancyfoot[C]{%
    \textcolor{demoRed}{\small%
      Generated by Claude Sonnet 4.6 via Amplify Skillsets
      $\cdot$ No human editing $\cdot$ For demonstration only
      $\cdot$ \thepage%
    }%
  }%
}

% ── Custom macros ─────────────────────────────────────────────────────────────
\newcommand{\DS}{\Delta S}            % Entanglement Asymmetry
\newcommand{\DSA}{\Delta S_A}         % EA of subsystem A
\newcommand{\tM}{t_{\rm M}}           % Mpemba crossing time
\newcommand{\tD}{t^*}                 % DQPT critical time
\newcommand{\gc}{g_c}                 % critical field
\newcommand{\gf}{g_f}                 % final field
\newcommand{\gi}{g_i}                 % initial field
\newcommand{\NA}{N_A}                 % subsystem size
\newcommand{\rhoA}{\rho_A}            % reduced density matrix
\newcommand{\calO}{\mathcal{O}}       % order symbol
\newcommand{\sgn}{\operatorname{sgn}} % sign function
\newcommand{\Tr}{\operatorname{Tr}}   % trace
\newcommand{\ke}[1]{\ket{#1}}         % ket
\newcommand{\br}[1]{\bra{#1}}         % bra
\newcommand{\hbin}{h_{\rm bin}}       % binary entropy
% frac position
\newcommand{\fracT}{\phi}             % fractional DQPT position

% ── Theorem environments (in case analytical arguments are added) ─────────────
\usepackage{amsthm}
\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{definition}{Definition}[section]
\newtheorem{remark}{Remark}[section]
